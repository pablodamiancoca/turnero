diff --git a/src/main/java/com/turnero/controller/TurnoApiController.java b/src/main/java/com/turnero/controller/TurnoApiController.java
index d2c5cd8..87bf3f6 100644
--- a/src/main/java/com/turnero/controller/TurnoApiController.java
+++ b/src/main/java/com/turnero/controller/TurnoApiController.java
@@ -1,7 +1,10 @@
 package com.turnero.controller;
 
+import com.turnero.dto.ConsultaDniResponse;
+import com.turnero.dto.TomarTurnoRequest;
 import com.turnero.model.Turno;
 import com.turnero.service.ColaService;
+import com.turnero.service.ConsultaDniService;
 import com.turnero.service.SseService;
 import com.turnero.service.TurnoInfo;
 import org.springframework.http.MediaType;
@@ -17,16 +20,28 @@ public class TurnoApiController {
 
     private final ColaService colaService;
     private final SseService sseService;
+    private final ConsultaDniService consultaDniService;
 
-    public TurnoApiController(ColaService colaService, SseService sseService) {
+    public TurnoApiController(ColaService colaService, SseService sseService, ConsultaDniService consultaDniService) {
         this.colaService = colaService;
         this.sseService = sseService;
+        this.consultaDniService = consultaDniService;
     }
 
-    // El cliente presiona "Tomar turno" al llegar
+    // Busca el nombre asociado a un DNI antes de tomar el turno.
+    // Si no se encuentra, el cliente carga el nombre a mano en la UI.
+    @GetMapping("/dni/{dni}")
+    public ConsultaDniResponse buscarPorDni(@PathVariable String dni) {
+        return consultaDniService.buscarNombrePorDni(dni)
+                .map(nombre -> new ConsultaDniResponse(dni, nombre, true))
+                .orElse(new ConsultaDniResponse(dni, null, false));
+    }
+
+    // El cliente presiona "Tomar turno" al llegar, ya con el DNI y el nombre confirmado/corregido
     @PostMapping("/turnos")
-    public ResponseEntity<Map<String, Object>> tomarTurno(@PathVariable String slug) {
-        Turno turno = colaService.tomarTurno(slug);
+    public ResponseEntity<Map<String, Object>> tomarTurno(@PathVariable String slug,
+                                                            @RequestBody TomarTurnoRequest cuerpo) {
+        Turno turno = colaService.tomarTurno(slug, cuerpo.dni(), cuerpo.nombre());
         long posicion = colaService.obtenerEstadoActual(slug).enEspera();
         return ResponseEntity.ok(Map.of(
                 "id", turno.getId(),
@@ -35,7 +50,7 @@ public class TurnoApiController {
         ));
     }
 
-    // El receptor llama al siguiente cliente. "ventanilla" es opcional (nombre del mostrador/box/agente).
+    // El receptor llama al siguiente cliente desde uno de los botones de box (BOX 1..BOX 5)
     @PostMapping("/siguiente")
     public TurnoInfo llamarSiguiente(@PathVariable String slug,
                                       @RequestParam(required = false, defaultValue = "") String ventanilla) {
@@ -55,3 +70,4 @@ public class TurnoApiController {
         return sseService.suscribir(slug);
     }
 }
+
diff --git a/src/main/java/com/turnero/dto/ConsultaDniResponse.java b/src/main/java/com/turnero/dto/ConsultaDniResponse.java
new file mode 100644
index 0000000..d4bff5c
--- /dev/null
+++ b/src/main/java/com/turnero/dto/ConsultaDniResponse.java
@@ -0,0 +1,8 @@
+package com.turnero.dto;
+
+/**
+ * Respuesta de la búsqueda por DNI.
+ * "encontrado" indica si el proveedor de datos devolvió un nombre asociado;
+ * si es false, el frontend debe pedirle al cliente que cargue el nombre a mano.
+ */
+public record ConsultaDniResponse(String dni, String nombre, boolean encontrado) {}
diff --git a/src/main/java/com/turnero/dto/TomarTurnoRequest.java b/src/main/java/com/turnero/dto/TomarTurnoRequest.java
new file mode 100644
index 0000000..abc9cd9
--- /dev/null
+++ b/src/main/java/com/turnero/dto/TomarTurnoRequest.java
@@ -0,0 +1,4 @@
+package com.turnero.dto;
+
+/** Cuerpo que envía el cliente al tomar un turno, ya con el DNI y el nombre confirmado/corregido. */
+public record TomarTurnoRequest(String dni, String nombre) {}
diff --git a/src/main/java/com/turnero/model/Turno.java b/src/main/java/com/turnero/model/Turno.java
index 5870e4a..9fef2d7 100644
--- a/src/main/java/com/turnero/model/Turno.java
+++ b/src/main/java/com/turnero/model/Turno.java
@@ -22,6 +22,12 @@ public class Turno {
     @Column(length = 10)
     private String prefijo;
 
+    @Column(length = 20)
+    private String dni;
+
+    @Column(length = 150)
+    private String nombreCliente;
+
     @Enumerated(EnumType.STRING)
     @Column(nullable = false, length = 20)
     private EstadoTurno estado = EstadoTurno.ESPERANDO;
@@ -59,6 +65,12 @@ public class Turno {
     public String getPrefijo() { return prefijo; }
     public void setPrefijo(String prefijo) { this.prefijo = prefijo; }
 
+    public String getDni() { return dni; }
+    public void setDni(String dni) { this.dni = dni; }
+
+    public String getNombreCliente() { return nombreCliente; }
+    public void setNombreCliente(String nombreCliente) { this.nombreCliente = nombreCliente; }
+
     public EstadoTurno getEstado() { return estado; }
     public void setEstado(EstadoTurno estado) { this.estado = estado; }
 
diff --git a/src/main/java/com/turnero/service/ColaService.java b/src/main/java/com/turnero/service/ColaService.java
index dc89e63..b6f6798 100644
--- a/src/main/java/com/turnero/service/ColaService.java
+++ b/src/main/java/com/turnero/service/ColaService.java
@@ -30,9 +30,9 @@ public class ColaService {
                 .orElseThrow(() -> new EntityNotFoundException("No existe una empresa con slug: " + slug));
     }
 
-    /** El cliente toma un nuevo número al llegar al local. */
+    /** El cliente toma un nuevo número al llegar al local, con su DNI y nombre ya confirmado/corregido. */
     @Transactional
-    public Turno tomarTurno(String slug) {
+    public Turno tomarTurno(String slug, String dni, String nombre) {
         Empresa empresa = buscarEmpresa(slug);
         LocalDate hoy = LocalDate.now();
 
@@ -51,6 +51,8 @@ public class ColaService {
         turno.setEmpresa(empresa);
         turno.setNumero(siguienteNumero);
         turno.setPrefijo(empresa.getPrefijo());
+        turno.setDni(dni);
+        turno.setNombreCliente(nombre);
         turno.setEstado(EstadoTurno.ESPERANDO);
         turno.setFecha(hoy);
         turno.setCreadoEn(LocalDateTime.now());
@@ -103,9 +105,9 @@ public class ColaService {
         long enEspera = turnoRepository.countByEmpresaAndFechaAndEstado(empresa, hoy, EstadoTurno.ESPERANDO);
 
         if (actual == null) {
-            return new TurnoInfo(null, "--", "SIN_TURNOS", null, (int) enEspera);
+            return new TurnoInfo(null, "--", "SIN_TURNOS", null, (int) enEspera, null, null);
         }
         return new TurnoInfo(actual.getId(), actual.getEtiqueta(), actual.getEstado().name(),
-                actual.getVentanilla(), (int) enEspera);
+                actual.getVentanilla(), (int) enEspera, actual.getDni(), actual.getNombreCliente());
     }
 }
diff --git a/src/main/java/com/turnero/service/ConsultaDniService.java b/src/main/java/com/turnero/service/ConsultaDniService.java
new file mode 100644
index 0000000..f33b546
--- /dev/null
+++ b/src/main/java/com/turnero/service/ConsultaDniService.java
@@ -0,0 +1,19 @@
+package com.turnero.service;
+
+import java.util.Optional;
+
+/**
+ * Punto de conexión con un proveedor externo de datos de personas físicas (DNI -> nombre).
+ *
+ * IMPORTANTE: no existe una API pública/gratuita "oficial" del RENAPER/ANSES para esto.
+ * Antes de usar esto en producción hay que contratar un proveedor autorizado (por ejemplo,
+ * un servicio de validación de identidad/KYC) y cumplir con la Ley 25.326 de Protección de
+ * Datos Personales: informar al titular, pedir consentimiento cuando corresponda, y resguardar
+ * el dato (el DNI es un dato personal).
+ *
+ * Esta interfaz permite reemplazar la implementación de ejemplo (ConsultaDniServiceMock)
+ * por la de tu proveedor real sin tocar el resto de la aplicación.
+ */
+public interface ConsultaDniService {
+    Optional<String> buscarNombrePorDni(String dni);
+}
diff --git a/src/main/java/com/turnero/service/ConsultaDniServiceMock.java b/src/main/java/com/turnero/service/ConsultaDniServiceMock.java
new file mode 100644
index 0000000..5dda991
--- /dev/null
+++ b/src/main/java/com/turnero/service/ConsultaDniServiceMock.java
@@ -0,0 +1,24 @@
+package com.turnero.service;
+
+import org.springframework.stereotype.Service;
+
+import java.util.Optional;
+
+/**
+ * Implementación de EJEMPLO. No consulta ninguna base real de personas.
+ *
+ * Reemplazá esta clase por la integración con tu proveedor autorizado de datos
+ * (por ejemplo, un servicio de validación de identidad contratado), implementando
+ * la interfaz ConsultaDniService. Mientras tanto, esta versión simplemente no
+ * encuentra a nadie, por lo que el cliente siempre va a tener que cargar su
+ * nombre manualmente en /{slug}/tomar — el flujo de la UI funciona igual.
+ */
+@Service
+public class ConsultaDniServiceMock implements ConsultaDniService {
+
+    @Override
+    public Optional<String> buscarNombrePorDni(String dni) {
+        // A propósito no devuelve datos: evita simular una fuente real de información personal.
+        return Optional.empty();
+    }
+}
diff --git a/src/main/java/com/turnero/service/TurnoInfo.java b/src/main/java/com/turnero/service/TurnoInfo.java
index bf4fb64..aa3514a 100644
--- a/src/main/java/com/turnero/service/TurnoInfo.java
+++ b/src/main/java/com/turnero/service/TurnoInfo.java
@@ -9,5 +9,7 @@ public record TurnoInfo(
         String etiqueta,
         String estado,
         String ventanilla,
-        int enEspera
+        int enEspera,
+        String dni,
+        String nombreCliente
 ) {}
diff --git a/src/main/resources/templates/panel.html b/src/main/resources/templates/panel.html
index 182c3d2..f157609 100644
--- a/src/main/resources/templates/panel.html
+++ b/src/main/resources/templates/panel.html
@@ -22,12 +22,18 @@
         <div class="tarjeta">
             <div class="etiqueta">Atendiendo</div>
             <div class="numero-actual" id="numero-actual">--</div>
+            <div class="info-espera" id="info-nombre"></div>
+            <div class="info-espera" id="info-dni"></div>
             <div class="info-espera" id="info-espera">Personas en espera: --</div>
 
-            <label for="ventanilla" style="margin-top:20px; text-align:left;">Tu nombre / ventanilla (opcional)</label>
-            <input type="text" id="ventanilla" placeholder="Ej: Box 2, Ana">
-
-            <button class="boton" id="btn-siguiente">Llamar siguiente</button>
+            <label style="margin-top:20px; text-align:left;">Elegí tu box para llamar al siguiente cliente</label>
+            <div class="panel-fila" id="botones-box">
+                <button class="boton secundario" data-box="BOX 1">BOX 1</button>
+                <button class="boton secundario" data-box="BOX 2">BOX 2</button>
+                <button class="boton secundario" data-box="BOX 3">BOX 3</button>
+                <button class="boton secundario" data-box="BOX 4">BOX 4</button>
+                <button class="boton secundario" data-box="BOX 5">BOX 5</button>
+            </div>
         </div>
         <div class="pie">
             <a th:href="${'/' + empresa.slug}" style="color:inherit;">Ver pantalla pública</a>
@@ -40,14 +46,19 @@
         function pintar(datos) {
             document.getElementById('numero-actual').textContent = datos.etiqueta;
             document.getElementById('info-espera').textContent = 'Personas en espera: ' + datos.enEspera;
+            document.getElementById('info-nombre').textContent = datos.nombreCliente ? datos.nombreCliente : '';
+            document.getElementById('info-dni').textContent = datos.dni ? ('DNI: ' + datos.dni) : '';
         }
 
         fetch('/api/' + slug + '/actual').then(r => r.json()).then(pintar);
 
-        document.getElementById('btn-siguiente').addEventListener('click', () => {
-            const ventanilla = document.getElementById('ventanilla').value.trim();
-            const url = '/api/' + slug + '/siguiente' + (ventanilla ? ('?ventanilla=' + encodeURIComponent(ventanilla)) : '');
-            fetch(url, { method: 'POST' }).then(r => r.json()).then(pintar);
+        document.querySelectorAll('#botones-box button').forEach(btn => {
+            btn.addEventListener('click', () => {
+                const box = btn.getAttribute('data-box');
+                fetch('/api/' + slug + '/siguiente?ventanilla=' + encodeURIComponent(box), { method: 'POST' })
+                    .then(r => r.json())
+                    .then(pintar);
+            });
         });
     </script>
 </body>
diff --git a/src/main/resources/templates/pantalla.html b/src/main/resources/templates/pantalla.html
index 4135d9b..35d8bb4 100644
--- a/src/main/resources/templates/pantalla.html
+++ b/src/main/resources/templates/pantalla.html
@@ -22,6 +22,8 @@
         <div class="tarjeta">
             <div class="etiqueta">Atendiendo ahora</div>
             <div class="numero-actual" id="numero-actual">--</div>
+            <div class="info-espera" id="info-nombre"></div>
+            <div class="info-espera" id="info-dni"></div>
             <div class="info-espera" id="info-ventanilla"></div>
             <div class="info-espera" id="info-espera">Personas en espera: --</div>
         </div>
@@ -38,6 +40,8 @@
             document.getElementById('numero-actual').textContent = datos.etiqueta;
             document.getElementById('info-espera').textContent = 'Personas en espera: ' + datos.enEspera;
             document.getElementById('info-ventanilla').textContent = datos.ventanilla ? ('Ventanilla: ' + datos.ventanilla) : '';
+            document.getElementById('info-nombre').textContent = datos.nombreCliente ? datos.nombreCliente : '';
+            document.getElementById('info-dni').textContent = datos.dni ? ('DNI: ' + datos.dni) : '';
         }
 
         // Estado inicial al cargar la página
diff --git a/src/main/resources/templates/tomar.html b/src/main/resources/templates/tomar.html
index f644df0..2800ef0 100644
--- a/src/main/resources/templates/tomar.html
+++ b/src/main/resources/templates/tomar.html
@@ -21,7 +21,20 @@
     <div class="contenedor">
         <div class="tarjeta">
             <p th:text="${empresa.mensajeBienvenida}">Bienvenido</p>
-            <button class="boton" id="btn-tomar">Tomar turno</button>
+
+            <div id="paso-dni">
+                <label for="dni" style="text-align:left;">Ingresá tu DNI</label>
+                <input type="text" id="dni" inputmode="numeric" placeholder="Ej: 30123456" maxlength="10">
+                <button class="boton" id="btn-buscar">Buscar</button>
+            </div>
+
+            <div id="paso-nombre" style="display:none; text-align:left;">
+                <p id="mensaje-nombre"></p>
+                <label for="nombre">Nombre y apellido</label>
+                <input type="text" id="nombre" placeholder="Nombre completo">
+                <button class="boton" id="btn-tomar">Confirmar y tomar turno</button>
+                <button class="boton secundario" id="btn-cambiar-dni" type="button">Cambiar DNI</button>
+            </div>
 
             <div class="ticket-tomado" id="ticket-tomado">
                 <div class="etiqueta">Tu número es</div>
@@ -36,18 +49,55 @@
 
     <script th:inline="javascript">
         const slug = /*[[${empresa.slug}]]*/ 'demo';
-        const boton = document.getElementById('btn-tomar');
+        let dniActual = '';
+
+        document.getElementById('btn-buscar').addEventListener('click', () => {
+            const dni = document.getElementById('dni').value.trim();
+            if (!dni) { return; }
+            dniActual = dni;
+
+            fetch('/api/' + slug + '/dni/' + encodeURIComponent(dni))
+                .then(r => r.json())
+                .then(datos => {
+                    const mensaje = document.getElementById('mensaje-nombre');
+                    const inputNombre = document.getElementById('nombre');
+                    if (datos.encontrado) {
+                        inputNombre.value = datos.nombre;
+                        mensaje.textContent = '¿Es correcto este nombre? Si no, corregilo antes de continuar.';
+                    } else {
+                        inputNombre.value = '';
+                        mensaje.textContent = 'No encontramos ese DNI. Ingresá tu nombre completo.';
+                    }
+                    document.getElementById('paso-dni').style.display = 'none';
+                    document.getElementById('paso-nombre').style.display = 'block';
+                })
+                .catch(() => alert('No se pudo verificar el DNI. Intentá nuevamente.'));
+        });
 
-        boton.addEventListener('click', () => {
+        document.getElementById('btn-cambiar-dni').addEventListener('click', () => {
+            document.getElementById('paso-nombre').style.display = 'none';
+            document.getElementById('paso-dni').style.display = 'block';
+        });
+
+        document.getElementById('btn-tomar').addEventListener('click', () => {
+            const nombre = document.getElementById('nombre').value.trim();
+            if (!nombre) { alert('Ingresá tu nombre para continuar.'); return; }
+
+            const boton = document.getElementById('btn-tomar');
             boton.disabled = true;
-            fetch('/api/' + slug + '/turnos', { method: 'POST' })
+
+            fetch('/api/' + slug + '/turnos', {
+                method: 'POST',
+                headers: { 'Content-Type': 'application/json' },
+                body: JSON.stringify({ dni: dniActual, nombre: nombre })
+            })
                 .then(r => r.json())
                 .then(datos => {
                     document.getElementById('numero-obtenido').textContent = datos.etiqueta;
                     document.getElementById('posicion-obtenida').textContent =
                         'Personas delante tuyo: ' + Math.max(0, datos.enEspera - 1);
                     document.getElementById('ticket-tomado').classList.add('visible');
-                    boton.style.display = 'none';
+                    document.getElementById('paso-nombre').style.display = 'none';
                 })
                 .catch(() => {
                     alert('No se pudo tomar el turno. Intentá nuevamente.');
